<div class="<%= menu_classes %>"
      role="menu"
      aria-orientation="vertical"
      tabindex="-1"
      inert
      data-filter-dropdown-target="menu">
  <% if title %>
    <div class="px-4 py-3 text-sm font-semibold text-gray-900 dark:text-white border-b border-gray-200 dark:border-gray-700">
      <%= title %>
    </div>
  <% end %>

  <%= content %>
</div>
